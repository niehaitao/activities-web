apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "react-activities.fullname" . }}-test-connection"
  labels:
    {{- include "react-activities.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "react-activities.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
